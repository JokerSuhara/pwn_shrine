---
import { url } from "@utils/url-utils";

export function getStaticPaths() {
	return [];
}
---

<!doctype html>
<html lang="zh-CN">
	<head>
		<meta charset="utf-8" />
		<meta http-equiv="refresh" content={`0; url=${url("/records/")}`} />
		<link rel="canonical" href={url("/records/")} />
		<title>正在跳转...</title>
	</head>
	<body>
		<p>正在跳转到 <a href={url("/records/")}>/records/</a>...</p>
	</body>
</html>
